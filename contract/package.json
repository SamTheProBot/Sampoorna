{
  "scripts": {
    "newDeploy": "rm -rf ./ignition/deployments && rm -rf cache && rm -rf artifacts && bun hardhat ignition deploy ignition/modules/deploy.ts --network localhost",
    "Deploy": "bun hardhat ignition deploy ignition/modules/deploy.ts --network localhost"
  },
  "dependencies": {
    "@nomicfoundation/hardhat-ignition-ethers": "^0.15.6",
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.12"
  }
}
